local class = require("30logglobal")

local environment = class()

environment._name = "environment"

function environment:_init(aName, aHeight, aWidth)
	self.name = AaNme
	self.height = aHeight
	self.width = aWidth
end


return environment